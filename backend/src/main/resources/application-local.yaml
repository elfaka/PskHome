spring:
  data.redis.host: pskhome-mem-1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://pskhome-db-1/page
    username: root
    password: ${MYSQL_ROOT_PASSWORD}