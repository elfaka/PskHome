name: pskhome

services:
  be:
    image: openjdk:21-jdk-slim
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ../backend/build/libs:/opt/libs
    working_dir: /opt/libs
    entrypoint:
      - sh
      - -euc
      - |
        java -jar build/libs/*.jar
